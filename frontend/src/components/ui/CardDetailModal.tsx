import React from 'react';

/**
 * CardDetailModal — disabled.
 * Generic popup cards removed per design requirements.
 * Information is now integrated directly into pages.
 */
export const CardDetailModal: React.FC = () => {
  return null;
};
